doctype html
html(lang="en")
  head
    title #{title}
    link(href='/css/styles.css', rel='stylesheet')
  body
    div
      if registered
        p #{login}, Вы уже вошли!  
      if login
        p #{login} уже существует. Выберете другой id или 
          a(href='/logout') зайдите 
      else
        form(action = "/signup" method = "POST")
          input(
            name = "login" 
            type = "text" 
            required 
            placeholder = "ID Пользователя"
          ) 
          br
          input(
            name = "password" 
            type = "password" 
            required 
            placeholder = "Пароль"
          )
          br
          button(type = "Submit") Регистрация